import { defineConfig } from "vite";

// Front-end only. `base: "./"` makes the build work on GitHub Pages,
// Netlify, or any static host without extra config.
export default defineConfig({
  base: "./",
});
